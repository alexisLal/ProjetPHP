{{ form_start(form) }}
<div class="d-flex justify-content-center">
	<div>
		<div>
			{{ form_widget(form.q) }}
			<button class="btn btn-primary" type="submit" id="btn-addon2">
				<i class="fa-solid fa-magnifying-glass"></i>
			</button>
		</div>
	</div>
</div>
{{ form_end(form) }}
